{
  "name": "nuxt-demo",
  "version": "1.0.0",
  "description": "My premium Nuxt.js project",
  "author": "linf",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=8888 NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env PORT=8888 NODE_ENV=production node server/index.js",
    "start-temp": "cross-env PORT=8889 NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "koa": "^2.6.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
